# @carbonenginejs/runtime-const

Shared constant vocabulary for CarbonEngineJS.

This package owns canonical string tokens and numeric enum mirrors used across
format readers, runtime-resource, and engine adapters. It does not parse files,
manage resource lifecycle, or create GPU objects.

It owns shared constant boundaries and naming conventions, rather than
format-specific parsing or runtime-resource behavior.
